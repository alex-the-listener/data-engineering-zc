[project]
name = "homework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alex-the-listener", email = "amsmusicworks@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "psycopg2-binary>=2.9.12",
    "pyarrow>=25.0.1",
    "requests>=2.34.2",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
homework = "homework:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
